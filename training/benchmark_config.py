"""
行人追踪对比试验 — 配置文件

直接修改下面的变量，然后运行:
    python run_tracking_benchmark.py

所有路径均相对于 training/ 目录，或使用绝对路径。
"""

# ============================================================
# 数据集
# ============================================================

# MOT20 训练集根目录
MOT_ROOT = "D:/Datasets/MOT20/train"

# 要评估的序列（None = 自动发现全部）
SEQUENCES = ["MOT20-01", "MOT20-02", "MOT20-03", "MOT20-05"]

# ============================================================
# 检测模型（可定义任意多个）
# name: 表格中显示的名称
# path: 权重文件路径
# ============================================================

MODELS = [
    {
        "name": "Baseline (YOLOv8n)",
        "path": "../runs/Sampled_100/exp0_baseline/weights/best.pt",
    },
    {
        "name": "改进模型 (Exp4 Full)",
        "path": "../runs/Sampled_100/exp4_full/weights/best.pt",
    },
]

# ============================================================
# 追踪器（可选: botsort, bytetrack, strongsort）
# ============================================================

TRACKERS = ["botsort", "strongsort"]

# ============================================================
# 检测参数
# ============================================================

CONF = 0.25       # 检测置信度阈值
IOU = 0.45        # NMS IoU 阈值
IMGSZ = 1280      # 推理分辨率

# ============================================================
# StrongSORT 专用
# ============================================================

REID_WEIGHTS = "osnet_x0_25_msmt17.pt"  # ReID 权重路径
DENSE_MODE = False                        # 密集场景参数预设

# ============================================================
# 评估参数
# ============================================================

IOU_THRESHOLD = 0.5  # 评估时的 IoU 匹配阈值（MOT Challenge 标准 = 0.5）

# ============================================================
# 输出
# ============================================================

OUTPUT_DIR = "./benchmark_results"  # 结果输出目录
SAVE_TRACKS = False                 # 是否保存每帧追踪结果为 MOT 格式文件
